@model IEnumerable<ApiANEPC.Models.cidade>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.cidade_nome)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.cidade_nome)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.idcidade }) |
            @Html.ActionLink("Details", "Details", new { id=item.idcidade }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.idcidade })
        </td>
    </tr>
}

</table>
