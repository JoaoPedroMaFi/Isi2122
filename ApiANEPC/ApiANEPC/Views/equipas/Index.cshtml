@model IEnumerable<ApiANEPC.Models.equipa>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.equipa_nome)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.cidade.cidade_nome)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.equipa_nome)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.cidade.cidade_nome)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.idequipa }) |
            @Html.ActionLink("Details", "Details", new { id=item.idequipa }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.idequipa })
        </td>
    </tr>
}

</table>
